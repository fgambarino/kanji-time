import { ResultMessagePipe } from './result-message.pipe';

describe('ResultMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new ResultMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
